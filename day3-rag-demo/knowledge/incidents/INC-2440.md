---
service: checkout
doc_type: incident
version: closed
---
# INC-2440 — Intermittent double charge on retried payments

## What happened
Before idempotency keys (see ADR-007), a gateway timeout plus a client retry
produced two captures for one basket, intermittently, under load. 500s appeared
on the refund path when support unwound the duplicates.
