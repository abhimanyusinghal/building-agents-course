# Azure OpenAI — chat + embeddings
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=                       # https://<resource>.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=                     # chat deployment (ask.py's agent)
AZURE_EMBEDDING_DEPLOYMENT=                  # e.g. a text-embedding-3-large deployment
OPENAI_API_VERSION=2025-04-01-preview

# price sheet for the transcripts, $ per 1K tokens (optional)
PRICE_IN_PER_1K=
PRICE_OUT_PER_1K=
